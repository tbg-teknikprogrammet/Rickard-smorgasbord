// todo application

// create empty array
// add event listener
// as long as the input is not quit the app should continiue to run
// use while(), if() - elseif(), foreach(), for(), push(), pop(), slice(),

Array
Button